#!/usr/bin/env perl

use v5.42;

use FindBin qw($RealBin);
BEGIN { $ENV{PERL_CARMEL_PATH} //= "$RealBin/.."; }
use Carmel::Setup;
use lib "$RealBin/../lib";

use DoubleDrive::App;

my $app = DoubleDrive::App->new();
$app->run();
